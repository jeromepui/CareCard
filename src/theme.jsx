import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00a17b",
    },
    secondary: {
      main: "#00c78e",
    },
    error: {
      main: "#e57373",
    },
  },
});

export default theme;